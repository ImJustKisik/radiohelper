# Интеграция изображений в RadioHelper

## Как это работает

### Streamlit редактор (`guide_editor.py`)
1. **Загрузка изображений**: При добавлении или редактировании протокола можно загрузить изображение укладки (PNG, JPG, JPEG, GIF)
2. **Хранение**: Изображения сохраняются в папку `guide_images/` с именем вида `{protocol_id}.{расширение}`
3. **Путь в JSON**: В поле `imageUrl` сохраняется путь `guide_images/{имя_файла}`

### Flask сервер (`app.py`)
- Добавлен маршрут `/guide_images/<path:filename>` 
- Приложение может скачивать изображения по URL: `http://localhost:5000/guide_images/ct_head_standard.png`

### Мобильное приложение (Android)
- Приложение использует `AsyncImage` из Coil для загрузки изображений по HTTP
- При открытии деталей протокола изображение загружается автоматически (если `imageUrl` не null)

## Шаги для добавления изображений

### 1. Локально на PC
```powershell
cd c:\Users\pizzalover\Desktop\radiohelper
streamlit run guide_editor.py
```

### 2. В браузере (http://localhost:8501)
- **📊 Просмотр**: видишь список протоколов с колонкой 🖼️ (наличие изображения)
- **➕ Добавить**: добавь новый протокол + загрузи изображение укладки
- **✏️ Редактировать**: выбери протокол → загрузи новое изображение или замени существующее

### 3. Проверка в приложении
- Запусти Flask: `python app.py`
- Открой приложение на телефоне (или эмуляторе) → Справочник → выбери регион
- Нажми на любой протокол → должно загрузиться изображение (если оно добавлено)

## Структура файлов

```
radiohelper/
├── app.py                 # Flask с роутом /guide_images/<filename>
├── guide.json             # Содержит поле imageUrl с путями
├── guide_editor.py        # Streamlit редактор
├── guide_images/          # 📁 Папка с изображениями (автоматически создаётся)
│   ├── ct_head_standard.png
│   ├── ct_abdomen_contrast.jpg
│   └── ...
└── ...
```

## Примеры путей в guide.json

```json
{
  "id": "ct_head_standard",
  "title": "КТ головного мозга стандартная",
  "imageUrl": "guide_images/ct_head_standard.png",    // ✅ Правильно
  "description": "..."
}
```

## Форматы поддерживаемых изображений
- PNG (рекомендуется для диаграмм)
- JPG / JPEG (для фотографий укладки)
- GIF (для анимаций)

## Рекомендации
- **Размер файла**: 50-500 KB (оптимально для мобильного)
- **Разрешение**: 800×600 px минимум, 1600×1200 px максимум
- **Содержание**: Фото укладки пациента, схемы расположения датчиков, диаграммы сканирования

## Если изображение не загружается в приложении

1. **Проверь Flask сервер**: убедись что `app.py` запущен на порту 5000
2. **Проверь наличие файла**: открой браузер → `http://localhost:5000/guide_images/ct_head_standard.png`
   - Если видишь изображение → проблема в приложении
   - Если 404 → проверь что файл в правильной папке
3. **Проверь guide.json**: поле `imageUrl` должно быть не null и содержать правильный путь
4. **Очисти кэш приложения** на телефоне → переустанови APK

## Обновление на GitHub (будущее)

Когда будешь синхронизировать с GitHub:
```bash
git add guide.json guide_images/
git commit -m "Update protocols with images"
git push
```

Потом в Android приложении обновить `GuideRepository.kt`:
```kotlin
private val baseUrl = "https://raw.githubusercontent.com/YOUR_USERNAME/radiohelper-guide/main/"
```

И изображения будут загружаться прямо с GitHub.
