# RadioHelper Android App

Мобильное приложение для радиологов и рентгенлаборантов: калькулятор эффективной дозы + справочник методик КТ/Рентгена.

## 🎯 Возможности

- **Калькулятор дозы** — быстрый расчёт эффективной дозы (мЗв) на основе DLP
- **Справочник методик** — 22+ протокола КТ и Рентгена с фотографиями укладок
- **Фильтрация** — выбор по области тела (голова, грудь, живот и т.д.)
- **Поиск** — быстрый поиск по названию методики
- **Офлайн** — работает без интернета (используя встроенные данные)
- **Онлайн обновления** — автоматическое скачивание новых методик с GitHub
- **Material Design 3** — современный интерфейс

## 📦 Технологический стек

- **Язык**: Kotlin 1.9.23
- **UI фреймворк**: Jetpack Compose
- **Архитектура**: MVVM с StateFlow
- **Сетевой запросы**: Retrofit 2.9.0
- **Парсинг JSON**: Gson 2.10.1
- **Загрузка изображений**: Coil 2.6.0
- **Навигация**: Navigation Compose 2.8.3
- **Material3**: 1.3.1

## 🚀 Сборка

### Требования
- Android SDK 24+ (API Level)
- Android Studio Koala или новее
- JDK 11+

### Debug APK

```bash
cd mobile\android-app
.\gradlew.bat :app:assembleDebug
# APK: app/build/outputs/apk/debug/app-debug.apk
```

### Release APK

```bash
cd mobile\android-app
.\gradlew.bat :app:assembleRelease
# APK: app/build/outputs/apk/release/app-release.apk
```

## 📋 Структура кода

```
app/src/main/
├── java/com/radiohelper/dosecalc/
│   ├── MainActivity.kt              # Главная активность + навигация
│   ├── dose/                        # Экран калькулятора дозы
│   └── guide/                       # Справочник методик
│       ├── GuideModels.kt           # Data classes
│       ├── GuideApi.kt              # Retrofit интерфейс
│       ├── GuideRepository.kt       # Загрузка данных
│       ├── GuideViewModel.kt        # StateFlow состояние
│       ├── BodySelector.kt          # Выбор области тела
│       ├── GuideListScreen.kt       # Список методик
│       └── ProtocolDetailScreen.kt  # Детали методики
├── res/
│   ├── raw/guide.json              # Встроенные методики
│   └── ...
└── AndroidManifest.xml
```

## 🔧 Конфигурация

### Обновление источника методик

Отредактируй `GuideRepository.kt`:

```kotlin
private val baseUrl = "https://raw.githubusercontent.com/ImJustKisik/radiohelper/main/"
```

Изменить на свой GitHub репозиторий если нужно.

### Минимальная версия Android

В `app/build.gradle.kts`:
```kotlin
minSdk = 24  // Android 7.0
targetSdk = 34  // Android 14
```

## 📱 Функциональность

### Калькулятор дозы
1. Выбери регион (Голова/Туловище)
2. Вводи DLP
3. Выбери возраст
4. Получи результат в мЗв

### Справочник методик
1. Нажми "Справочник" в нижней навигации
2. Выбери область тела (голова, грудь, живот и т.д.)
3. Используй фильтры:
   - 📷 **Рентген** — рентгеновские исследования
   - 🔲 **КТ** — компьютерная томография
4. Используй поиск по названию методики
5. Нажми на методику для просмотра:
   - Фотографии укладки
   - Технических параметров (kV, mAs)
   - Подробного описания техники

## 🔄 Обновления методик

Приложение автоматически проверяет новые методики каждый раз при открытии справочника.

**Процесс:**
1. Приложение загружает `guide.json` из GitHub
2. Если версия новее → сохраняет в локальный кэш
3. При отсутствии интернета → использует встроенные данные

## 🐛 Troubleshooting

**Сборка не работает:**
```bash
# Очисти build cache
.\gradlew.bat clean

# Попробуй ещё раз
.\gradlew.bat :app:assembleDebug
```

**Методики не загружаются:**
- Проверь интернет соединение
- Убедись что `baseUrl` в `GuideRepository.kt` правильный
- Проверь что `guide.json` существует на GitHub

**Изображения не видны:**
- Убедись что `guide_images/` залита на GitHub
- Проверь что путь в `imageUrl` совпадает с именем файла

## 📝 API Интеграция

### GuideApi.kt
```kotlin
interface GuideApi {
    @GET("guide.json")
    suspend fun fetchGuide(): GuideData
}
```

Загружает `https://raw.githubusercontent.com/ImJustKisik/radiohelper/main/guide.json`

## 🎨 Дизайн

- **Цветовая схема**: Material Design 3 (динамические цвета)
- **Шрифты**: Системные шрифты Android
- **Иконки**: Material Icons
- **Адаптивность**: работает на всех размерах экранов

## 📄 Лицензия

MIT

## 👨‍💻 Автор

ImJustKisik

---

Для веб-приложения и методик: https://github.com/ImJustKisik/radiohelper
